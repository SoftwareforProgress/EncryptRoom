# AGENTS

When making code changes in this repository:

1. Run tests with:
   - `go test ./...`
2. Run lint/static checks with:
   - `go vet ./...`
3. Include a short summary of command results in your final response.
